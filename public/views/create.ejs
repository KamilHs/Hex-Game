<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Game</title>
    <%- include('partials/styles') %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
        integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/create.css">
    <script src="https://cdn.jsdelivr.net/npm/p5@1.1.9/lib/p5.js"></script>
</head>

<body>
    <header>
        <h1 class="heading">Create New Game</h1>
    </header>

    <main>
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-md-8">
                    <form action="/create" class="create-game-form" method="POST">
                        <div class="d-flex">
                            <div class="create-game-form__group w-25 p-1 mb-3">
                                <label for="first_player_color" class="create-game-form__label">Player 1</label>
                                <input id="first_player_color" name="first_player_color" type="color"
                                    value="<%= config.firstPlayerColor %>" class="create-game-form__control w-100">
                            </div>
                            <div class="create-game-form__group w-25 p-1 mb-3">
                                <label for="second_player_color" class="create-game-form__label">Player 2</label>
                                <input id="second_player_color" name="second_player_color" type="color"
                                    value="<%= config.secondPlayerColor %>" class="create-game-form__control w-100">
                            </div>
                            <div class="create-game-form__group w-25 p-1 mb-3">
                                <label for="border_color" class="create-game-form__label">Border</label>
                                <input id="border_color" name="border_color" type="color"
                                    value="<%= config.borderColor %>" class="create-game-form__control w-100">
                            </div>
                            <div class="create-game-form__group w-25 p-1 mb-3">
                                <label for="bgc_color" class="create-game-form__label">Background</label>
                                <input id="bgc_color" name="bgc_color" type="color" value="<%= config.emptyBackground%>"
                                    class="create-game-form__control w-100">
                            </div>
                        </div>
                        <div class="create-game-form__group p-1">
                            <label for="board_size" class="create-game-form__label">Board Size</label>
                            <input class="create-game-form__control w-100" min="<%= config.minSize %>"
                                max="<%= config.maxSize %>" value="<%= config.boardSize %>" step="1" type="range"
                                name="board_size" id="board_size">
                        </div>

                        <div id="canvas-container"></div>

                        <div class="create-game-form__submit-container p-1">
                            <button class="create-game-form__submit" type="submit">Create New Game</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </main>

    <script src="/scripts/models/hexagon.js"></script>
    <script src="/scripts/models/board.js"></script>
    <script src="/scripts/create.js"></script>
</body>

</html>